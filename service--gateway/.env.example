# Chat storage: PostgreSQL
CHAT_STORAGE__URI=postgresql+asyncpg://gateway:gateway@localhost:15432/gateway

# Chat storage: filesystem (uncomment to use instead)
# CHAT_STORAGE__URI=/path/to/chats
# CHAT_STORAGE__EXTRA__ARCHIVE_DIR=/path/to/archive
